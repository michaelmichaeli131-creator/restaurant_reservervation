<% layout("_layout", it) %>

<%
  function tt(k, fb){
    try{
      if (it && typeof it.t==='function'){
        const s = it.t(k);
        if(!s || s===k || s===`(${k})`) return fb;
        return s;
      }
    }catch(e){}
    return fb;
  }

  const restaurant = it.restaurant || {};
  const ingredients = Array.isArray(it.ingredients) ? it.ingredients : [];
  const suppliers  = Array.isArray(it.suppliers) ? it.suppliers : [];

  function esc(s){ return String(s ?? ""); }

  const activeSuppliers = suppliers.filter(s => s && s.isActive !== false);
%>

<section class="sb-owner container" aria-label="מלאי חומרי גלם">
  <div class="card">
    <header class="owner-hd">
      <div>
        <h1>מלאי חומרי גלם · <%= restaurant.name || 'מסעדה' %></h1>
        <p class="muted">
          ניהול חומרי גלם בסיסי: כמות, מינימום, עלות ליחידה וספק מועדף.
          ההזמנות מהספקים יעדכנו את המלאי אוטומטית כשמסמנים "סופק".
        </p>
      </div>
      <div class="actions">
        <a class="btn ghost sm" href="/owner/<%= restaurant.id %>/inventory/suppliers">📦 ספקים</a>
        <a class="btn ghost sm" href="/owner/<%= restaurant.id %>/inventory/purchase_orders">🧾 הזמנות רכש</a>
        <a class="btn ghost sm" href="/owner/<%= restaurant.id %>/inventory/costs">📈 עלויות</a>
      </div>
    </header>

    <!-- טופס יצירת חומר גלם חדש -->
    <div class="panel card" style="margin-top:14px">
      <h2 class="card-title"><%= tt('owner.inventory.add_new', 'הוספת חומר גלם חדש') %></h2>

      <form class="form row" method="post" action="/owner/<%= restaurant.id %>/inventory/stock/ingredient">
        <input type="hidden" name="id" value="">

        <label class="lbl">
          <%= tt('owner.inventory.form.name', 'שם חומר גלם') %> *
          <input name="name" required placeholder="<%= tt('owner.inventory.placeholder.name', 'לדוגמה: קמח לבן 00') %>">
        </label>

        <label class="lbl">
          <%= tt('owner.inventory.form.unit', 'יחידת מידה') %>
          <input name="unit" placeholder="ק״ג / ליטר / יח׳" value="ק״ג">
        </label>

        <label class="lbl">
          <%= tt('owner.inventory.form.current_qty', 'כמות נוכחית') %>
          <input name="currentQty" type="number" step="0.01" placeholder="0">
        </label>

        <label class="lbl">
          <%= tt('owner.inventory.form.min_qty', 'מינימום רצוי') %>
          <input name="minQty" type="number" step="0.01" placeholder="0">
        </label>

        <label class="lbl">
          <%= tt('owner.inventory.form.cost_per_unit', 'עלות ליחידה (₪)') %>
          <input name="costPerUnit" type="number" step="0.01" placeholder="<%= tt('common.optional', 'אופציונלי') %>">
        </label>

        <label class="lbl">
          <%= tt('owner.inventory.form.supplier', 'ספק מועדף') %>
          <select name="supplierId">
            <option value="">— <%= tt('common.none', 'ללא') %> —</option>
            <% activeSuppliers.forEach(function(s){ %>
              <option value="<%= esc(s.id) %>"><%= esc(s.name) %></option>
            <% }) %>
          </select>
        </label>

        <label class="lbl" style="min-width:220px;flex:1">
          <%= tt('owner.inventory.form.notes', 'הערות') %>
          <input name="notes" placeholder="<%= tt('owner.inventory.placeholder.notes', 'מידע נוסף: סוג, אחסון, אלרגנים וכו׳') %>">
        </label>

        <button class="btn primary sm" type="submit">➕ <%= tt('owner.inventory.btn_add', 'הוסף חומר גלם') %></button>
      </form>
    </div>

    <!-- טבלת כל חומרי הגלם -->
    <div class="panel card" style="margin-top:16px">
      <h2 class="card-title"><%= tt('owner.inventory.all_items', 'כל חומרי הגלם') %></h2>

      <% if (!ingredients.length) { %>
        <p class="muted"><%= tt('owner.inventory.empty', 'עדיין אין חומרי גלם. הוסף חומר גלם ראשון למעלה.') %></p>
      <% } else { %>

        <div class="table">
          <div class="tr th">
            <div><%= tt('owner.inventory.form.name', 'שם') %></div>
            <div><%= tt('owner.inventory.form.current_qty', 'כמות נוכחית') %></div>
            <div><%= tt('owner.inventory.form.min_qty', 'מינימום') %></div>
            <div><%= tt('owner.inventory.form.cost_per_unit', 'עלות ליח׳') %></div>
            <div><%= tt('owner.inventory.form.supplier', 'ספק מועדף') %></div>
            <div></div>
          </div>

          <% ingredients.forEach(function(ing){ %>
            <form class="tr" method="post" action="/owner/<%= restaurant.id %>/inventory/stock/ingredient">
              <input type="hidden" name="id" value="<%= esc(ing.id) %>">

              <!-- שם + הערות -->
              <div class="td">
                <input class="in" name="name" required value="<%= esc(ing.name || '') %>">
                <div class="muted small" style="margin-top:4px">
                  יחידה:
                  <input
                    class="in in-inline"
                    name="unit"
                    value="<%= esc(ing.unit || '') %>"
                    placeholder="ק״ג / ליטר / יח׳"
                  >
                </div>
                <% if (ing.notes) { %>
                  <div class="muted small" style="margin-top:4px">
                    הערות: <%= esc(ing.notes) %>
                  </div>
                <% } %>
              </div>

              <!-- כמות נוכחית -->
              <div class="td">
                <input
                  class="in"
                  name="currentQty"
                  type="number"
                  step="0.01"
                  value="<%= esc(ing.currentQty ?? '') %>"
                  placeholder="0"
                >
              </div>

              <!-- מינימום -->
              <div class="td">
                <input
                  class="in"
                  name="minQty"
                  type="number"
                  step="0.01"
                  value="<%= esc(ing.minQty ?? '') %>"
                  placeholder="0"
                >
              </div>

              <!-- עלות ליחידה -->
              <div class="td">
                <input
                  class="in"
                  name="costPerUnit"
                  type="number"
                  step="0.01"
                  value="<%= esc(ing.costPerUnit ?? '') %>"
                  placeholder="—"
                >
              </div>

              <!-- ספק מועדף -->
              <div class="td">
                <select class="in" name="supplierId">
                  <option value="">— ללא —</option>
                  <% activeSuppliers.forEach(function(s){ %>
                    <option
                      value="<%= esc(s.id) %>"
                      <%= (ing.preferredSupplierId === s.id ? 'selected' : '') %>
                    >
                      <%= esc(s.name) %>
                    </option>
                  <% }) %>
                </select>
                <% if (ing.supplierName && !ing.preferredSupplierId) { %>
                  <div class="muted small" style="margin-top:4px">
                    ספק (legacy): <%= esc(ing.supplierName) %>
                  </div>
                <% } %>
              </div>

              <!-- כפתורים -->
              <div class="td right">
                <button class="btn primary sm" type="submit">שמור</button>

                <button
                  class="btn ghost sm danger"
                  type="submit"
                  formaction="/owner/<%= restaurant.id %>/inventory/stock/ingredient/<%= esc(ing.id) %>/delete"
                  formmethod="post"
                  onclick="return confirm('למחוק את חומר הגלם הזה?')"
                  style="margin-right:8px"
                >
                  מחיקה
                </button>
              </div>
            </form>
          <% }) %>
        </div>

      <% } %>
    </div>
  </div>
</section>

<style>
  .sb-owner.container{max-width:1100px;margin:20px auto;padding:0 16px}

  .card{
    background: var(--panel, #121622);
    border:1px solid var(--bd, #222834);
    border-radius:18px; padding:18px; margin-bottom:18px;
    color: var(--ink, #e6e8ef);
    box-shadow: 0 10px 30px rgba(0,0,0,.35);
  }
  .panel.card{height:100%}

  .owner-hd{display:flex;align-items:flex-start;gap:12px;justify-content:space-between}
  .card-title{ margin:0 0 10px; font-size: clamp(16px, 2.2vw, 20px) }
  .muted{ color: var(--ink-muted, #9aa3b2); }
  .small{ font-size:12px; }

  .actions{display:flex;gap:8px;flex-wrap:wrap}
  .btn{
    display:inline-flex; align-items:center; justify-content:center; gap:8px;
    padding:10px 14px; border-radius:12px; cursor:pointer; text-decoration:none;
    border:1px solid var(--bd, #23293a); background:transparent;
    color: var(--ink, #e6e8ef); font-weight:700;
    transition: transform .08s ease, filter .12s ease, box-shadow .12s ease;
  }
  .btn:hover{ transform: translateY(-1px); }
  .btn.primary{
    background: var(--brand, #3b82f6); color: var(--brand-ink, #fff); border-color:transparent;
    box-shadow: 0 8px 24px rgba(59,130,246,.35);
  }
  .btn.ghost{background:rgba(15,23,42,.6);}
  .btn.sm{ padding:8px 12px; border-radius:10px; font-size:13px }
  .btn.danger{
    border-color: rgba(248,113,113,.35);
    color:#fecaca;
  }

  .row{display:flex;gap:10px;align-items:flex-end;flex-wrap:wrap}
  .lbl{display:block;font-weight:700;font-size:13px}
  .lbl input, .lbl select{
    width:200px; padding:10px 12px; margin-top:6px;
    background:#171b29; color:#e6e8ef; border:1px solid #23293a; border-radius:12px;
    font-size:14px; outline:none;
  }

  .lbl select{height:40px;}

  .in{
    width:100%;
    padding:10px 12px;
    background:#171b29;color:#e6e8ef;border:1px solid #23293a;border-radius:12px;
    font-size:14px; outline:none;
  }
  .in-inline{
    width:auto; display:inline-block; margin-top:0; margin-left:4px;
  }
  .in:focus{
    border-color: color-mix(in srgb, var(--brand, #3b82f6) 60%, #23293a);
    box-shadow: 0 0 0 3px color-mix(in srgb, var(--brand, #3b82f6) 20%, transparent);
  }

  .table{margin-top:10px;border:1px solid rgba(148,163,184,.18);border-radius:14px;overflow:hidden}
  .tr{display:grid;grid-template-columns:1.8fr .9fr .9fr .9fr 1.2fr 1.1fr;gap:10px;align-items:center;padding:10px 12px;border-top:1px solid rgba(255,255,255,.06)}
  .tr:first-child{border-top:none}
  .th{background:rgba(148,163,184,.08);font-weight:900;color:#dbe3f0}

  .td{min-width:0}
  .td.right{display:flex;justify-content:flex-end;gap:8px;flex-wrap:wrap}

  @media (max-width:950px){
    .tr{grid-template-columns:1fr}
    .td.right{justify-content:flex-start}
    .lbl input, .lbl select{width:100%}
  }
</style>
