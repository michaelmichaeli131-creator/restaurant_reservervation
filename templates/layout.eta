<!doctype html>
<html lang="he" dir="rtl">
  <head>
    <meta charset="utf-8" />
    <title><%= it.title || "GeoTable" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="/static/styles.css" />
  </head>
  <body>
    <header class="site-header">
      <nav class="nav">
        <a class="brand" href="/">GeoTable</a>

        <div class="nav-links">
          <% if (it.user && it.user.role === "owner") { %>
            <a class="link" href="/owner">אזור מנהלים</a>
          <% } %>

          <% if (!it.user) { %>
            <a class="btn" href="/login">התחברות</a>
            <a class="btn secondary" href="/signup">הרשמה</a>
            <a class="btn accent" href="/owner/login">כניסת מנהלים</a>
          <% } else { %>
            <form method="post" action="/logout" style="display:inline">
              <button class="btn danger" type="submit">התנתקות</button>
            </form>
          <% } %>
        </div>
      </nav>
    </header>

    <main class="container">
      <%~ it.body %>
    </main>

    <footer class="site-footer">
      <small>© <%= new Date().getFullYear() %> GeoTable</small>
    </footer>
  </body>
</html>
