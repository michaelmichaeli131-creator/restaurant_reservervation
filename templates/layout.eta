<!doctype html>
<html lang="he" dir="rtl">
  <head>
    <meta charset="utf-8" />
    <title><%= it.title || "GeoTable" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="/static/styles.css" />
  </head>
  <body>
    <header class="site-header">
      <nav class="nav">
        <a class="brand" href="/">GeoTable</a>

        <div class="nav-links">
          <% if (it.user && it.user.role === "owner") { %>
            <a class="link" href="/owner"><%= (it.t && it.t('nav.admin_area')) || 'Admin Area' %></a>
          <% } %>

          <% if (it.user) { %>
            <span class="user-chip">
              <%= (it.t && it.t('nav.greeting')) || 'Hello,' %> <strong><%= it.user.firstName || it.user.username || it.user.email %></strong>
            </span>
            <form class="inline" method="post" action="/logout">
              <button class="btn secondary" type="submit"><%= (it.t && it.t('nav.logout')) || 'Logout' %></button>
            </form>
          <% } else { %>
            <a class="link" href="/login"><%= (it.t && it.t('nav.login')) || 'Login' %></a>
            <a class="link" href="/auth/register"><%= (it.t && it.t('nav.register')) || 'Register' %></a>
          <% } %>
        </div>
      </nav>
    </header>

    <main class="container">
      <%~ it.body %>
    </main>

    <footer class="site-footer">
      <small>© <%= new Date().getFullYear() %> GeoTable</small>
    </footer>
  </body>
</html>
