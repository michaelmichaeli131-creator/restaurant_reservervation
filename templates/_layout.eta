<!doctype html>
<html lang="<%= it.lang || 'he' %>" dir="<%= it.dir || 'rtl' %>">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <meta name="color-scheme" content="dark light">
  <title><%= it.title ? it.title + ' · SpotBook' : 'SpotBook' %></title>

  <!-- טען את העיצוב הישן רק אם זה לא אדמין -->
  <% if (!it.admin) { %>
    <link rel="stylesheet" href="/static/styles.css?v=<%= it.BUILD_TAG || '' %>"/>
  <% } %>

  <!-- עיצוב כהה חדש של SpotBook (נטען תמיד ואחרון) -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@300;400;500;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/public/css/spotbook.css?v=<%= it.BUILD_TAG || Date.now() %>"/>

  <%~ it.head || '' %>
  <style>
    /* בלוק קטן לשפות מתחת ל-Admin */
    .lang-under-admin{display:block; margin-top:8px; font-size:.9rem; opacity:.95}
    .lang-under-admin .lang-label{margin-bottom:4px; color:var(--muted,#94a3b8); line-height:1.2}
    .lang-under-admin .lang-links{display:flex; gap:10px; flex-wrap:wrap}
    .lang-under-admin .lang-links a{
      text-decoration:none; padding:4px 8px; border-radius:999px;
      border:1px solid var(--bd,#2a2f3a); background:rgba(255,255,255,.03)
    }
    .lang-under-admin .lang-links a:hover{background:rgba(255,255,255,.07)}
  </style>
</head>
<body class="sb-body<%= it.admin ? ' admin' : '' %>" data-theme="<%= it.admin ? 'dark' : 'auto' %>">
  <header class="sb-header">
    <div class="sb-container sb-header-row">
      <!-- לוגו -->
      <a class="brand" href="/" aria-label="SpotBook">
        <img class="brand-logo" src="/public/img/logo-spotbook.png" alt="SpotBook Logo"/>
      </a>

      <!-- שורת המשתמש: מתחת ללוגו בפועל -->
      <% if (it.user) { %>
      <div class="user-mobile">
        מחובר/ת:
        <strong><%= [it.user.firstName, it.user.lastName].filter(Boolean).join(" ") %></strong>
      </div>
      <% } %>

      <!-- פעולות (ניהול / התחברות / התנתקות / אדמין) -->
      <nav class="sb-actions" aria-label="Main actions">
        <% if (it.user) { %>
          <a class="btn primary btn-manage" href="/owner">נהל מסעדה</a>
          <a class="btn ghost btn-logout" href="/auth/logout">התנתקות</a>
        <% } else { %>
          <a class="btn ghost" href="/auth/login">התחברות</a>
          <a class="btn" style="border-color:transparent;background:var(--brand);color:var(--brand-ink)" href="/auth/register">הרשמה</a>
        <% } %>
        <a class="btn ghost btn-admin" href="/admin/login">Admin</a>

        <!-- שפות – מתחת ל-Admin, קישורים פשוטים -->
        <div class="lang-under-admin">
          <div class="lang-label">שפה:</div>
          <div class="lang-links">
            <a href="/lang/he" aria-label="עברית">🇮🇱 עברית</a>
            <a href="/lang/en" aria-label="English">🇬🇧 English</a>
            <a href="/lang/ka" aria-label="ქართული">🇬🇪 ქართული</a>
          </div>
        </div>
      </nav>
    </div>
  </header>

  <main class="sb-container">
    <%~ it.body %>
  </main>

  <footer class="sb-footer">
    <p>© <%= new Date().getFullYear() %> SpotBook · כל הזכויות שמורות</p>
  </footer>

  <!-- סקריפט ישן (אם צריך), עם גרסה -->
  <script src="/static/app.js?v=<%= it.BUILD_TAG || '' %>" defer></script>

  <!-- הזרקת סקריפטים נוספים לפי דף -->
  <% if (it.scripts) { it.scripts.forEach(function(src){ %>
    <script src="<%= src %>" type="module"></script>
  <% }) } %>
</body>
</html>
